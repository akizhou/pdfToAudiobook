import com.MainWindow;
import javax.swing.*;

public class PDF2Audiobook {
    public static void main(String args[]) {
        MainWindow mainWindow = new MainWindow(); //need to pass reference of circuitBoard into GUI.
        mainWindow.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //Close the app only if closing plot parameters
    }
}